{
  "installCommand": "yarn install",
  "buildCommand": "DISABLE_ESLINT_PLUGIN=true CI=false yarn build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
